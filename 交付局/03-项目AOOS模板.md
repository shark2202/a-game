---
type: Component Template
title: 项目军 AOOS 模板（软件版 v0.1.2）
description: 全量通道项目军的组织设计模板。复制本模板至 组织运行记录/<项目军名>-AOOS.md 后填写。政策继承块自动生效（00 §5，含元层全文内嵌），不得删除任何节；Actor Registry 行按 05 路由表选配（未配角色的行可删，不构成删节）。
timestamp: 2026-09-16T15:50:00+08:00
---

# <项目军名> · AOOS v0.1（模板）

> **Goal（Locked）**：<可判定 AC，逐条列出；负例：做了不算达成的情况；优先级>
> **需求稳定声明**：goal-lock 时点 <时间>；AC 级变更走 goal-change-relock（变更账本见 §七；受影响波次即时 HALTED）；预期变更率 <低/中/高——高则本军不适用此模板，回 C1>
> **边界（硬约束）**：<不修改什么；不可逆操作清单（如有，逐项列明——审批走 T3b 不可逆队列）；回退方式>
> **成军前检查**：已查注册表（读取时点 <时间>），无在航同目标组织。通道分类已经 goal-keeper 确认（<时间>）。目标进入时间戳：<时间>（三字段计时起点）。
> **形态与裁剪**：按 05 路由表选形态 <微型/标准/联邦/hotfix/重构/写作>；对照 05 裁剪矩阵确定本军对 M0–M4 各段的取舍（本节必填，防全段误配）。

## 一、Actor Registry（行按 05 形态配置选配；scout 是否前置见裁剪矩阵）

| Actor | 类型 | Assignment |
|---|---|---|
| goal-keeper（人类） | human | 异步终审：分类确认 ✓ / 批准成军 ✓ / 例外处理（待触发；停机类即时，不可逆入 T3b 队列）/ 终审（待触发） |
| coordinator（成军官兼任；繁重单波可派波次司令，协议见 00 §8.2） | ai-orchestrator | 单写者；注册表/任务书/带宽记账；治理钩子（例外路由/验证额度/安全停机） |
| scout（按裁剪矩阵） | ai-worker | M0 三类事实：依赖可得性/风险清单/目标尺度现实性；只报事实 |
| builder ×N（写作军为 researcher ×N） | ai-worker（每人只写自己模块目录） | 单模块实现；溯源→实现→自检落盘；任务书自包含 |
| verifier | ai-worker（fresh，无共享上下文） | 波次验证与终局审计；自写脚本实际运行，输出落盘 projects/<项目名>/验证/ |
| hostile-reviewer（中型以上＝标准军/联邦军必设；重构军必设） | ai-worker（fresh） | 攻击设计与产出；判决三选一 |

**判定者派发计划**＝本表 verifier / hostile-reviewer 行（含派发时点：波次验证门/终局审计/设计审查）——T2 批准 AOOS 即完成派发确认（元层 C5 不可委托内核），成军官仅执行派发，波次司令无派发判定者权限。

## 二、Role 要点（四要素）

<responsibility / authority / constraints 各一行；至少覆盖 coordinator、builder（或 researcher）、verifier 三者。参照 04-角色构件库.md 选型，勿自创常设角色。>

## 三、模块所有权表 + 波次表（编码职能的载体）

**模块所有权**（政策 single-writer-per-module，一行一模块一写者）：

| 模块 | 目录/文件范围 | 写者 | 依赖（上游模块） |
|---|---|---|---|

**波次表**（每波一行；带试点的形态试点波必为波 1；裁剪矩阵豁免者无试点波）：

| 波次 | 范围 | builder | 交付物 | 验证门（fresh? 抽样率） | 人类触点 |
|---|---|---|---|---|---|
| 1（试点，如适用） | 风险最高切片 | | 可运行切片 + 教训清单 | fresh 全验 | 无（教训回灌后进下波，coordinator 记录） |
| 2…N | | | | | |

**全量任务书批准时锁定波次总数：<N>**（§七 前提失效阈值的分母）。

## 四、DecisionRecord（事前）

<按 02-缓存库 CMP-002 模板：决策一句话 / 维度 / 死法（必含「协调者自身失效」预演）/ 可逆性（含已扩散引用回收清单）/ 事前预测 P1–Pn>

## 五、Runtime Loop + 降级拓扑预置（三类必填）

标准循环：Goal→分析→选人→授权→执行→验证→学习→重构（M0–M4 段图见 00 §3）。

- **限流类**（过载/token 限额）：<降级路径，如：波次内 builder 减一，顺延任务，不停验证>
- **过滤类**（依赖/外部资源不可得）：<降级链，如：官方源→镜像→锁版本→scout 提目标尺度改写>
- **超界类**（验证失败率超阈/例外超频/敌意审查击穿>10）：<停波留痕 T3a 上浮；阈：<X>>
- **协调者不可达**（00 §8.2 D13）：波次司令就地停波留痕，等待成军官回合。

## 六、人类终审栏（批量终审队列行）

- [ ] 接受交付（授 Verified）
- [ ] 否决/返工（说明：______；连续 2 轮否决自动升级 T3a 验收僵局）
- [ ] 指示下一步（______）
- 终审材料：Pre-Verified 报告路径：projects/<项目名>/验收报告.md（T4 必做：抽查 ≥1 项运行输出与 projects/<项目名>/验证/ 盘上记录比对）

## 七、变更账本 + 事后校准

**变更账本**（00 §7 格式）：

| # | 变更内容 | 进入时间 | 是否 AC 级 | 受影响波次（编号+处置） | 处置（重过 M0/前提失效重分批/军内消化/拒绝） | 重锁耗时 |
|---|---|---|---|---|---|---|

**事后校准**（M4 回填）：

| 预测 | 结果（命中/半命中/翻车） | Surprise |
|---|---|---|

**三字段**：成军耗时 ____（起点＝目标进入时间戳）；缓存复用构件数 ____（口径：02 登记条目且本次实际引用）；本次新固化数 ____。
**触点当量消耗**：T1 _ 轮 / T2 _ 轮 / T3a _ 次 / T3b _ 轮 / T4 _ 轮（记入 06 账本）。