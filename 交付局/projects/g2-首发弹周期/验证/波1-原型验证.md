# 波 1 原型验证报告（fresh verifier · 三原型实测）

> 任务书：`任务书\波1-验证.md`（PAT-005）。验证者：fresh verifier，无共享上下文，只读只报告，不修复。
> 环境：Windows 11 / bash / node v24.16.0。运行时间：2026-09-16。
> 验证对象：`原型\1\`（擦弹脉冲 GRAZE PULSE）、`原型\2\`（引信合成 Fuse Merge）、`原型\3\`（红限反应堆 Redline Reactor）。
> 方法：全部代码逐文件人工审读 + `node --check` + `node test.js` 亲自实跑（退出码以本次运行为准），并对可复现性做二次运行哈希比对。

---

## 原型 1 · 擦弹脉冲 GRAZE PULSE（槽 1，Flappy 族变体）

### V1a 可玩性实测

**core 与渲染分离**：PASS
- `core.js` 全文（1-501 行）无任何 DOM 引用（无 `document`/`window` 除 UMD 挂载 `root.GrazePulse`，root 在浏览器为 window、Node 为 globalThis，core.js:16-22）；仅用 `Date.now()`（core.js:432,439）与 `Math.random`（默认 rng，core.js:116）。
- `index.html:18` `<script src="core.js">` 加载核心；渲染层（index.html:19-774）只做输入（index.html:257-260 pointerdown/keydown）、绘制、WebAudio 音效（index.html:49-106）、特效（index.html:108-132）。所有游戏规则（重力/碰撞/计分/阶段/生死）都在 core.js，渲染层仅读 `snapshot()`（core.js:444-463）绘制，无规则逻辑。

**node test.js 实跑**：PASS（退出码 0）
- `node --check core.js`、`node --check test.js` 均无报错。
- `node test.js` 实际输出（本次运行，节选）：

```
=== GRAZE PULSE · 擦弹脉冲 · core.js 机器自测 (node v24.16.0) ===
模拟局数: 120 局  (rookie/normal/expert 各 40 局, 固定种子可复现)

--- 胜负分布 ---
总体: 胜 80 (66.7%) / 负 40 (33.3%)
  rookie : 胜 7/40  (平均局时 155.0s)
  normal : 胜 36/40  (平均局时 147.0s)
  expert : 胜 37/40  (平均局时 132.4s)

--- 局时统计 ---
平均局时(全部): 144.8s
平均局时(胜局): 135.4s  [n=80]
平均局时(败局): 163.7s  [n=40]

--- 埋点事件触发计数 (core.js 同一埋点通道 getMetrics) ---
  death              305
  emergency_phase    26
  flap               78612
  gate_pass          3688
  graze              5052
  lose               40
  phase_burst        642
  retry              305
  run_end            120
  run_start          120
  session_end        1
  session_start      1
  stage_up           228
  wall_pass          86
  win                80

--- 指标汇总 ---
  session 开始/结束: OK / OK
  局数(runs): 120, 失败重试次数(retries): 305, 阶段内死亡(deaths): 305

--- 断言 (17/17 通过) ---
全部断言通过

RESULT: PASS (exit 0)
```

- 退出码 `EXIT_CODE=0`；两次重跑输出 md5 完全一致（`7877ddc3…`），固定种子可复现成立。输出与 README 自检记录逐字一致。

**一局有终态 + 30 秒可上手**：PASS
- 有胜负终态：通关 `distance >= TOTAL_DIST → endRun('win')`（core.js:365-368）；耗尽 5 颗重试珠 `endRun('lose')`（core.js:329-331）。一局 = 3 阶段 13500 距离（core.js:70-74），实测平均局时 144.8s（胜局 135.4s / 败局 163.7s），落在 2-5 分钟设计区间，非无尽空转。
- 30 秒可上手：唯一输入「点按/空格」（index.html:257-260），标题页 5 行一句话教程（index.html:637-643）。

### V1b 埋点真实性

**五要素存在且被触发覆盖**：PASS
- session 开始/结束：`startSession`/`endSession`（core.js:429-441），实测 `session_start=1 / session_end=1`。
- 局数：`metrics.runs++` + `run_start` 事件（core.js:232-233），实测 120。
- 单局时长：`metrics.runDurations.push(runClock)`（core.js:254），实测 120 条。
- 失败重试：阶段重生 `respawn()` 计 `retry(kind:'stage_respawn')`（core.js:274-275）+ 败局再开计 `retry(kind:'new_run')`（core.js:235），实测 305。
- 死亡：`die()` 计 `death`（core.js:266-267），实测 305。

**导出通道同一**：PASS
- core.js `getMetrics()`（core.js:466-485）为唯一实现；`exportMetrics` 即同一函数别名（core.js:495）。
- 浏览器：`window.__getMetrics = game.getMetrics`、`window.__metrics = game.getMetrics()`（index.html:193-194），每秒节流刷新（index.html:762-767）。
- test.js 直接 `game.getMetrics()`（test.js:123,132）——同一通道。

**造假检查（逐行读模拟逻辑）**：PASS，非造假
- test.js 模拟真实驱动 core：`game.startRun()`（test.js:121）→ 循环内 `game.flap()`（test.js:86,94,99）+ `game.update(dt)`（test.js:101）+ `game.snapshot()`（test.js:103）——胜利/时长/事件计数全部来自 core 真实推进结果，无硬编码胜率、无与 core 无关的随机编造。
- bot 只读公开 `snapshot()`（玩家位置/速度/障碍/能量），用抛物线预测决策（test.js:62-99），不读 core 私有状态作弊。
- 计数自洽性核算：`run_start=run_end=win+lose=120` ✔；`retries(305)=deaths(305)` 可推导——败局末次死亡不重生（40 局败局 → 265 次阶段重生重试）+ 败局后再开 40 次 = 305 ✔；`flap=78612`、`graze=5052→phase_burst=642→wall_pass=86→emergency_phase=26` 全链路触发，量级与 120 局模拟一致，不可能为编造。
- 三档 bot 胜率 17.5%/90%/92.5% 有梯度，随技能参数（margin/noise/interval，test.js:41-45）单调变化，符合真实模拟特征。

**瑕疵记录（不影响判定）**：
1. 断言计数显示「17/17」为硬编码分母（test.js:193 `17 - fails.length`/`17`），实际 `assert()` 调用为 15 处（test.js:173-190）。README「17 项断言」随之失准。属显示口径问题，非造假（15 项断言全部真实通过）。
2. `respawn()` 中 `runScore = Math.max(0, runScore - 0)`（core.js:277）为无操作表达式（分数保留），代码异味，无功能影响。

### V2 克隆判定

**差异声明成立**：PASS——非直接克隆
- README 声明的差异点逐项在代码中真实存在且可感知：
  - 双层碰撞（致命核 coreR=10 + 擦弹圈 grazeR=34）：core.js:33-34，命中判定分离 core.js:381-384，擦弹计分/充能 core.js:403-415；
  - 擦弹充能→相位脉冲→穿墙回能风险循环：energy 配置 core.js:50-55，`activatePhase` core.js:298-303，穿墙得分 core.js:387-394；
  - 满能被撞自动紧急相位免死：core.js:396-398（实测 emergency_phase=26 次触发）；
  - 擦弹 0.15s 缓冲豁免：core.js:47-49,408；
  - 局制结构（3 阶段 checkpoint + 5 重试珠 + 通关/失败终态）：core.js:64-74,273-296,329-331,365-368——与 Flappy Bird 的单命无尽跑分结构不同。
- 表现层（三境渐变色板/相位拖尾/擦弹圈虚线可视化，index.html:198-223,380-465）与数值（重力 1500、flap -430、能量槽等）均为原创设定，非 Flappy 数值复制。
- 结论：玩法机制（贴险换资源循环）+ 数值 + 表现三层均与最近似玩法（Flappy Bird）有实质差异，不构成「三同」直接克隆。

### V3 版权链抽查

**零外部资产**：PASS
- `index.html`：无 `<img>`/`<audio>`/`<link>` 外链，无 CDN，无网络请求；全部视觉为 Canvas 程序绘制（背景渐变/山脊/光尘/栅/粒子，index.html:278-504），音效为 WebAudio 程序合成（index.html:49-106）。字体使用系统字体栈（"PingFang SC"/"Microsoft YaHei"/Consolas 等，如 index.html:598,628），非外载字体文件。
- `core.js`：纯逻辑，无任何资源引用。
- `test.js`：无网络/文件依赖。

### 原型 1 总判定：**通过**

V1a / V1b / V2 / V3 全部 PASS。瑕疵 2 条仅记录在案（断言分母口径 17→实际 15；一处无操作表达式），不构成打回条件。整改建议（可选，不强制）：test.js 断言分母改为 `assertCount` 变量自计；清理 core.js:277 无操作表达式。

---

## 原型 2 · 引信合成 Fuse Merge（槽 2，合成/连锁型）

### V1a 可玩性实测

**core 与渲染分离**：PASS
- `core.js` 全文（1-428 行）无 DOM 依赖（仅 UMD 挂载 `root.FuseCore`/`root.__metrics`，root 为 window 或 globalThis，core.js:21-25,84）；时钟 `Date.now` 可注入（core.js:61-63）。
- `index.html:90` `<script src="core.js">` 加载核心；渲染层（index.html:91-405）只做绘制/动画/输入（键盘 index.html:369-378、滑动 380-398）/HUD/结算浮层，全部规则（压缩合并/引信燃烧/灰烬/胜负）在 core.js，渲染仅消费 `game.get()` 与 `game.move()` 返回结果。

**node test.js 实跑**：PASS（退出码 0）
- `node --check` core.js/test.js 均通过。
- `node test.js` 实际输出（本次运行）：`自检汇总: 34 项通过 / 0 项失败 · 结论: 全部通过 —— node test.js 退出码 0`，`EXIT_CODE=0`。关键数据：[A] 主模拟 120 局胜 81 (67.5%) / 负 39 (32.5%)，平均 162.7 步/局，平均局时(虚拟时钟) 3.23 分钟；[B] 埋点计数 session_start=1 / game_start=120 / game_end=120 / move=19520 / merge=17284 / stone_formed=1110 / stone_cleared=1038 / retry=38；[C] 难度扫描 maxSteps 140/180/240 → 胜率 10.0%/62.5%/95.0%；[D] 会话共 240 局 · 胜 148 · 失败重试 92 次。与 README 自检记录一致（仅「机器实耗」毫秒一项不同：本次 738ms vs README 963ms，该项本身随机器波动且 README 已声明）。
- 可复现性：两次重跑 diff 仅第 25 行「机器实耗仅 790/799 ms」不同（该行取自 `Date.now()` 实测），全部游戏结果/计数逐字一致——种子随机 + 虚拟时钟的确定性声明成立。

**一局有终态 + 30 秒可上手**：PASS
- 终态三路齐备：合成目标 `maxTileLevel >= targetLevel → won`（core.js:334）；无路可走 `→ lost(stuck)`（core.js:344-346）；总引信步数预算 `moves >= maxSteps(180) → lost(timeup)`（core.js:347-350）。规则抽检 0.8/0.9 直接验证两条终态路径（test.js:157-172）。单局步数硬上限 180（A6 断言），非无尽空转。
- 30 秒可上手：页首 4 行一句话教程（index.html:63-68），方向键/WASD/滑动即玩。

### V1b 埋点真实性

**五要素存在且被触发覆盖**：PASS
- session 开始/结束：`ensureSession`/`endSession`（core.js:67-80），实测 1/1（B1、D1）。
- 局数：`game_start`/`game_end`（core.js:369-370,402），实测 120+120（B 段）与 240+240（D 段）。
- 单局时长：`durationMs: clock() - startTs`（core.js:393）——浏览器为真实时间；node 下 test.js 注入 1.2s/步虚拟时钟（test.js:31-33,236），README 与输出均明示该口径，时长由真实步数×1.2s 导出，非编造。
- 失败重试：`retry` = 上一局败后再开新局（core.js:369），实测 38（B 段）/ 92（全会话）。
- 附带 move/merge/stone_formed/stone_cleared 全事件。

**导出通道同一**：PASS
- `root.__metrics = { getMetrics, endSession }`（core.js:84）与 `Core.getMetrics()` 读同一份内存 `metrics`（core.js:47-82）——浏览器即 `window.__metrics.getMetrics()`，node 即 `globalThis.__metrics`。
- test.js B10（test.js:319-327）实际读取 `globalThis.__metrics.getMetrics()` 与 `Core.getMetrics()` 对账一致——同通道有实测验证，非仅声明。

**造假检查（逐行读模拟逻辑）**：PASS，非造假
- bot 真实驱动 core：`playGame` 循环 `pickMove`（1-ply 前瞻：`Core.engine.clone(game.get())` + `applyMove` 试算四方向评分，test.js:217-227）→ `g.move(d)`（test.js:236）——真实走核心状态机，无硬编码胜负。
- [0] 段 12 条规则抽检用手工构造棋盘直接驱动引擎（test.js:59-192），逐条断言合并/灰烬/续命波/胜负/无效步语义。
- 埋点对账是本 test 的亮点：B3-B7 将事件计数与逐局记录求和逐项对账（move=Σmoves 19520=19520、merge=Σmerges 17284=17284、stone_formed=Σburnouts 1110=1110、stone_cleared=ΣstoneClears 1038=1038、retry=败局后续开局数 38=38）——计数与模拟自洽有显式验证。
- 独立核算：retry(92) = 全会话败局数（A 段 39 + C 段 36+15+2 = 92，末局为胜局无后续开局），与「败局后再开计一次」定义吻合 ✔；game_end(240) = 120 + 3×40 ✔。
- [C] 难度扫描显示胜率随 maxSteps 140→240 从 10%→95% 单调变化，参数真实作用于核心。

**瑕疵记录（不影响判定）**：
1. test.js:77-81 存在一个结果被丢弃的死 IIFE（首个「0.1 基础合并」试打块，无断言无输出），疑为开发残留，无功能影响。
2. A4 断言文案写「2–5 分钟设计带」，实际判定条件为 `estMin >= 1.5 && estMin <= 6`（test.js:279），带宽于文案；实测 3.23 分钟确在 2-5 内。文案与条件不一致，属口径瑕疵。

### V2 克隆判定

**差异声明成立**：PASS——非直接克隆
- README 声明的差异点逐项在代码中真实存在且经 [0] 段抽检验证：
  - 每块内嵌倒计时引信：fuse 字段 + 每步燃烧结算（core.js:308-319），新块短引信不受波保护（core.js:312 `if (t.fresh) { t.fuse-1; continue; }`，抽检 0.5）；
  - 续命波/十字止燃/连锁止燃：core.js:299-307,313-317（抽检 0.6 逐格验证 +1/0/-1 结算）；
  - 燃尽成灰烬障碍（不动/不融/分段压缩）：core.js:322-329、141-142（抽检 0.3/0.7）；合并落点震碎相邻灰烬：core.js:269-289（抽检 0.4a/0.4b）；
  - 总引信步数预算收口单局时长：core.js:347-350（抽检 0.9），对抗 2048 后期无限拖延。
- 数值层与 2048 不同：5×5 盘（2048 为 4×4）、目标合成 8 即胜（2048 为奔 2048 的无尽刷分）、出生块等级 1/2（2048 为 2/4）、灰烬/耐久/引信等参数均为原创系统。表现层（引信彩条/裂纹灰烬/续命波扫光）为原创 Canvas 绘制。
- 结论：机制（时间压力攻防 vs 纯空间管理）+ 数值 + 表现三层均有实质差异，不构成直接克隆。

### V3 版权链抽查

**零外部资产**：PASS
- `index.html`：无 `<img>`/`<audio>`/`<link>`/CDN/网络请求；全部视觉 Canvas 程序绘制（棋盘/方块/裂纹/动画，index.html:140-317），灰烬裂纹为按格确定性伪随机程序生成（index.html:127-138）；字体为系统字体栈（"PingFang SC"/"Microsoft YaHei"/"Segoe UI"/sans-serif，index.html:12）。
- `core.js`：纯逻辑零资源；`test.js`：零网络/文件依赖。

### 原型 2 总判定：**通过**

V1a / V1b / V2 / V3 全部 PASS。瑕疵 2 条仅记录在案（死代码 IIFE；A4 文案与断言带宽不一致），不构成打回条件。埋点对账（B3-B7 计数 vs 逐局求和）为三原型中最扎实。整改建议（可选，不强制）：删除 test.js:77-81 死代码；A4 断言条件与文案对齐。

---

## 原型 3 · 红限反应堆 Redline Reactor（槽 3，渐进/循环型 idle 族）

### V1a 可玩性实测

**core 与渲染分离**：PASS
- `core.js` 全文（1-362 行）零 DOM 依赖（grep 全文无 `document.`/`getElementById`/`querySelector`；仅 UMD 挂载 `global.RedlineReactor`/`global.__metrics`，global 为 window 或 globalThis，core.js:20,346,360）。
- `index.html:110` `<script src="core.js">` 加载核心；渲染层（index.html:111-505）只做绘制/动画/输入/按钮状态，规则（产出/热量/倍率/胜负）全部来自 `Core.createRun()` 返回的 ReactorRun（core.js:122-243），渲染层仅调用 `run.update(dt)` 与 `actStoke/actVent/actBank`（index.html:222-228,184-193）。

**node test.js 实跑**：PASS（退出码 0）
- `node --check` core.js/test.js 均通过。
- `node test.js` 实际输出（本次运行）：`RESULT: PASS (16/16 checks passed)`，`EXIT_CODE=0`。关键数据：120 局胜 73 (60.8%) / 负 47 (39.2%)，熔毁 15 / 超时 32；分水平 novice 17.1%（超时为主）/ decent 100% / expert 73.3%（熔毁 8）/ maniac 0%（熔毁 7 + 超时 3）；全体平均局时 221.8s（min 115.7 / max 250.0），胜局平均 223.5s；埋点 session_start=1 / session_end=1 / run_start=120 / run_end=120 / win=73 / loss=47 / retry=46 / stoke=755 / vent=1392 / bank=1988。与 README 自检记录逐字一致（仅 wallMs 33 vs 37，该项为机器墙钟实测，随机器波动，README 已声明口径）。
- 可复现性：两次重跑 diff 仅 wallMs 一行不同（34/36ms），全部结果/计数一致；test.js 内建「同种子回放确定性」检查（两遍模拟签名一致，test.js:90-101）亦为 PASS。

**一局有终态 + 30 秒可上手**：PASS
- 终态三路齐备且在本次实测中全部真实出现：入库达标 `banked >= VAULT_GOAL(4800) → won(goal)`（core.js:182,221）；熔毁 `heat >= 100 → lost(meltdown)`（core.js:178-181，实测 15 次）；超时 `time >= TIME_LIMIT(250s) → lost(timeout)`（core.js:183，实测 32 次）。单局被 250s 硬上限封顶（时长检查 max=250.0），非无尽空转。
- 30 秒可上手：开始界面 5 行教程（index.html:73-79）+ 页脚一句话上手（index.html:105），仅 3 个键（1/2/3），README 声明成立。

### V1b 埋点真实性

**五要素存在且被触发覆盖**：PASS
- session 开始/结束：`beginSession`/`endSession`（core.js:99-116），实测 1/1。
- 局数：`run_start`/`run_end`（core.js:257,229），实测 120/120。
- 单局时长：`runDurations` 按 run_end 事件 push（core.js:262-264），实测 120 条（all/win/loss 三口径 120/73/47）。
- 失败重试：`retry` = 上一局失败后的重开（core.js:254-255），实测 46。
- 动作事件 stoke/vent/bank 全非零（755/1392/1988）。

**导出通道同一**：PASS
- `global.__metrics = metricsApi`（core.js:348-360），其 `counters` 为 live getter 动态引用内存对象（core.js:356-359），`getMetrics()` 与 node 侧 `core.getMetrics()` 读同一份 `metrics`（core.js:294-319）——浏览器 `window.__metrics` 与 node 同一通道。
- index.html 实际消费该通道：页脚埋点行直接读 `window.__metrics.counters`（index.html:177-180，每 0.6s 刷新 index.html:503）。

**造假检查（逐行读模拟逻辑）**：PASS，非造假
- bot 真实驱动 core：`core.createRun()` 逐局创建（test.js:120），`simulate()` 以 dt=0.1s 步进 `run.update(0.1)`（test.js:80），决策点按人类反应间隔调用 `decide()` → `run.actStoke()/actVent()/actBank()`（test.js:49-65,84）——全部经核心状态机结算，胜负由 core 判定，无硬编码。
- 交叉校验 16 项：埋点计数 vs 本地独立统计（test.js:109-137 本地逐局记录，与 core 计数无共享变量）逐项对账——win/loss/meltdown/timeout/retry/时长总量（core=26610.4 = local 求和）全部一致；`win+loss=run_end` 自洽（73+47=120）。
- 独立核算：`retry(46) = 败局数(47) − 末局`（末局为 maniac 败局、无后续开局）✔；分水平胜负+失败原因求和 = 局数（novice 6+29=35、decent 45+0=45、expert 22+8=30、maniac 0+7+3=10）✔；meltdown(15)+timeout(32)=loss(47) ✔。
- 4 档 bot 胜率 17.1%→100%→73.3%→0% 的梯度（慢反应超时 / 稳健通关 / 贪进熔毁 / 无脑全灭）与各档阈值参数（SKILLS，test.js:39-44）语义一致，是真实模拟的行为特征。

**瑕疵记录（不影响判定）**：无实质瑕疵。仅注：时长以班次时限 250s 封顶（core.js:232，防步进浮点多一拍），口径在代码注释与 test.js:124-125 均有说明；README「一局 4 分钟」为 250s≈4:10 的口语化说法，与 UI 一致（index.html:83「4:10」）。

### V2 克隆判定

**差异声明成立**：PASS——非直接克隆
- README 声明的三个机制差异逐项在代码中真实存在且被实测触发：
  - ① 红热入库倍率（收益随危险度缩放）：`bankMult = 0.40 + 1.70×(heat/100)`（core.js:40-41,161-165），冷存重罚 ×0.40 / 红热 ×2.1；
  - ② 排热泄压但浪费 25% 当前电力（安全有价格）：core.js:39,198-207；
  - ③ 局内「核心不稳定」难度曲线：`t>100s` 起产热系数每 60s +45%（core.js:42-43,148-151）。
- 与最近似玩法（Cookie Clicker 族长线 idle）的结构性差异：单局闭环（250s 时限 + 4800 目标 + 熔毁线三重终态）对抗 idle 的「无限游戏、耐心必胜、等待零风险」；产热 ∝ 产出（core.js:33,152-154）使爬升速度本身成为风险源。数值（价格 ×1.9、产出 ×1.5、泄热 40+7n 等）与表现（反应堆核心/热量计/红晕 Canvas 绘制）均为原创。
- 结论：不构成直接克隆。

### V3 版权链抽查

**零外部资产**：PASS
- grep 全量扫描三对 index.html/core.js：无 `http(s)://`、无 `<img>/<audio>/<video>/<link>`、无 `@import/url()/cdn/fetch/XMLHttpRequest`（命中 0 条）。
- 全部视觉为 Canvas 程序绘制（反应堆核心辉光/热浪粒子/热量计/进度条，index.html:232-474）；字体为系统字体栈（"Segoe UI"/"Microsoft YaHei"/Consolas，index.html:9,343,381 等）；`core.js` 纯逻辑零资源。

### 原型 3 总判定：**通过**

V1a / V1b / V2 / V3 全部 PASS，无实质瑕疵。16 项交叉校验 + 内建确定性回放检查为三原型中验证密度最高者。

---

## 总汇总

| 原型 | V1a 可玩性 | V1b 埋点真实性 | V2 克隆判定 | V3 版权链 | node test.js（本次实跑） | 总判定 |
|---|---|---|---|---|---|---|
| 1 擦弹脉冲 GRAZE PULSE | PASS | PASS | PASS 非克隆 | PASS | exit 0（17/17 显示，实际 15 项断言全过） | **通过** |
| 2 引信合成 Fuse Merge | PASS | PASS | PASS 非克隆 | PASS | exit 0（34/34） | **通过** |
| 3 红限反应堆 Redline Reactor | PASS | PASS | PASS 非克隆 | PASS | exit 0（16/16） | **通过** |

**三原型均通过**。共性证据链：① 三份 test.js 均以真实 API 驱动各自 core（无硬编码胜率/计数、无与 core 无关的编造数据），退出码 0 且输出与 README 自检记录一致；② 埋点五要素（session 开始/结束、局数、单局时长、失败重试）三原型齐备且计数与模拟局数自洽（本报告逐项核算）；③ 三者均有胜负终态与 2-5 分钟局时长实证；④ 零外部资产（grep 全量扫描 0 命中）。仅原型 1 存在 2 条不影响判定的记录性瑕疵、原型 2 存在 2 条记录性瑕疵（详见各节）。

*（fresh verifier · 只读只报告 · 未对原型目录做任何写改）*
