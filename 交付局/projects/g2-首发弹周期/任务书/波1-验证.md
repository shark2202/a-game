# 波 1 验证任务书 · fresh verifier（三原型实测）

> 自包含任务书（PAT-005）。组织：G2-首个游戏发弹军。验证者：fresh，无共享上下文，只读只报告。
> **写者权限**：只写 `交付局\projects\g2-首发弹周期\验证\` 目录；对 `原型\1\、2\、3\` 三个目录**只读**——不得写/改/删任何文件。

## 待验证对象

`D:\bytecoder_ai\交付局\projects\g2-首发弹周期\原型\` 下三个子目录（1/2/3），各含 HTML5 小游戏原型：`index.html`（渲染层）、`core.js`（核心逻辑）、`test.js`（自测脚本）、`README.md`（钩子声明+自检记录）。

## 验证项（逐原型，逐项给 PASS/FAIL+证据）

**V1a 可玩性实测**：
- 读 `core.js` 确认核心与渲染分离（core 不依赖 DOM）；读 `index.html` 确认加载 core 且渲染层无游戏规则逻辑。
- 用 Bash `node` 实际运行各 `test.js`：记录退出码与输出。**不得只信 README 粘贴的输出**——亲自跑，输出以你本次运行为准。
- 静态审查 `core.js` + `index.html`：判定游戏是否可构成「一局」且有胜负/阶段终态（不是无尽无终态的空转）；前 30 秒可上手的路径是否成立（无教程或一句话教程）。

**V1b 埋点真实性**：
- 核对埋点五要素在代码中真实存在且被触发路径覆盖：session 开始/结束、局数、单局时长、失败重试次数。
- 核对 `window.__metrics` 导出接口与 test.js 读取同一通道（node 下经 module.exports）。
- **造假检查**：test.js 的模拟是否真实驱动 core（逐行读模拟逻辑），还是硬编码/随机生成与 core 无关的假数据。埋点计数与模拟局数是否自洽。

**V2 克隆判定**：
- README 差异声明是否成立：该原型与最近似知名玩法的差异点在代码/玩法中是否真实存在且可感知（非纯文案）。
- 判定是否构成「直接克隆」（玩法机制+数值+表现三同）。

**V3 版权链抽查**：
- 确认零外部资产：`index.html`/`core.js` 无外链图片/音频/字体/CDN 资源（允许 picsum 等占位类除外——发现即记录）；全部美术应为 Canvas 程序绘制。

## 报告格式

写 `验证\波1-原型验证.md`：逐原型×逐项（V1a/V1b/V2/V3）结论表+证据（引用文件与行号/你的实际运行输出粘贴）；末尾每原型一个总判定：**通过 / 打回（列整改点）**。只报告，不修复。

## 产出协议（PAT-007/PAT-008）

报告文件落盘后，最终回复只给一行：三原型总判定（如「1 通过/2 打回/3 通过」）。文件为准。
